<div class = "center-align">
League : <%= @league.name %>

<ul class = "center-align"> 
<% @league.teams.each do |team| %> 
   
   Teams: <%=link_to team.name , team_path(team.id) %> 

<%end %>
</ul>

   <% api["api"]["teams"].each do |team|  %>
      <p> <%= team["name"] %> </p>
    <% end %>
</div>
